import { theme } from '../../../styles';

export const calendarTheme = {
  arrowColor: theme.colors.gray['400'],
  selectedDayTextColor: theme.colors.white,
  dayTextColor: theme.colors.gray['600'],
  textDisabledColor: theme.colors.gray['200'],
  monthTextColor: theme.colors.gray['600'],
  todayTextColor: theme.colors.primary,
  textDayFontFamily: theme.fonts.inter.medium,
  textMonthFontFamily: theme.fonts.archivo.semiBold,
  textDayHeaderFontFamily: theme.fonts.archivo.semiBold,
  textDayFontSize: 15,
  textMonthFontSize: 20,
  textDayHeaderFontSize: 12,
};
